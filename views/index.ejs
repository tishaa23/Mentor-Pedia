<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Solr Query Results</title>
    <link rel="icon" type="image/x-icon" href="image.ico">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        .vh-100 {
            height: 100vh;
        }
        .container {
            padding-top: 3rem;
            padding-bottom: 3rem;
        }
        .card {
            border: none;
            border-radius: 1rem;
            background-color: #fff;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
            overflow: hidden;
            transition: all 0.3s ease;
        }
        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 30px rgba(4, 62, 223, 0.2);
        }
        .profile-image {
            width: 90%;
            height: auto;
            transition: transform 0.3s ease;
        }
        .profile-image:hover {
            transform: scale(1.05);
        }
        .btn-dark {
            color: #fff;
            background-color: #24028b;
            border-color: #002e5c;
            margin-top: 1rem;
        }
        .btn-dark:hover {
            background-color: #0059b1;
            border-color: #085d9e;
        }
        .expanded-content {
            max-height: 0;
            overflow: hidden;
            transition: max-height 0.5s ease;
        }
        .open .expanded-content {
            max-height: 500px;
        }

        @keyframes cardFadeIn {
            0% {
                opacity: 0;
                transform: translateY(-50px);
            }

            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .card-animation {
            animation: cardFadeIn 0.5s ease-in-out;
        }
    </style>
</head>

<body>
    <section class="vh-100">
        <div class="container py-5">
            <div class="row justify-content-center align-items-center">
                <!-- Profile Cards -->
                <% for(var i=0; i < data2.docs.length; i++) { %>
                    <div class="col-lg-5 mb-3 card-animation">
                        <div class="card profile-card">
                            <div class="row g-0">
                                <div class="col-md-4 profile-image-container">
                                    <img src="<%= data2.docs[i].image %>" alt="" onerror="this.onerror=null; this.src='./blank-profile-picture-973460_960_720.webp';" class="img-fluid my-5 rounded-circle profile-image" />
                                </div>
                                <div class="col-md-8">
                                    <div class="card-body p-4">
                                        <h5 class="card-title">
                                            <%= data2.docs[i].name %>
                                        </h5>
                                        <p class="card-text">
                                            <%= data2.docs[i].position %>
                                        </p>
                                        <ul class="list-unstyled">
                                            

                                            <!-- <li>Email: <%= data2.docs[i].email %> </li> -->
                                            <!-- <li>Department: <%= data2.docs[i].dept %> -->
                                            <li>Department: <%= data2.docs[i].department_s_ %>
                                            </li>
                                            <li>office_phone: <%= data2.docs[i].office_phone %>
                                            </li>
                                        </ul>

                                        <button class="btn btn-dark" onclick="toggleCardBody(this)">View
                                            Profile</button>

                                        <div class="expanded-content">
                                            <hr>
                                            <p>Research Areas:</p>
                                            <ul>
                                                <!-- research_interests -->
                                                <!-- research_areas --> 
                                                <% for (var j=0; j < data2.docs[i].research_interests.length; j++) { %>
                                                    <li>
                                                        <%= data2.docs[i].research_interests[j] %>
                                                    </li>
                                                    <% } %>
                                            </ul>
                                            <p>Additional information about <%= data2.docs[i].name[0] %>...</p>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <% } %>
            </div>
        </div>
    </section>

    <script>
        function toggleCardBody(button) {
            const cardBody = button.closest('.card-body');
            cardBody.classList.toggle('open');
        }
    </script>
</body>

</html>
